REACT_APP_API_BASE_URL="http://localhost:8000"
 
# ── Evolution API (required for WhatsApp module) ──────────────────────────────
# Must match AUTHENTICATION_API_KEY in .env.evolution exactly
EVOLUTION_API_KEY=netkhata_evolution_key_2026_x7K9f3Vb2QyZ6tR
 
# URL that your Flask app uses to reach the Evolution API container
# If Flask runs on the host machine (not in Docker): http://localhost:8081
# If Flask runs inside Docker on the same network:   http://evolution-api:8080
EVOLUTION_API_URL=http://localhost:8081
DATABASE_CONNECTION_URI=postgresql://postgres:M.m03007493358@host.docker.internal/isp_management?schema=evolution
